# Funding links shown on the repo's "Sponsor" button.
#
# Left fully commented on purpose: an enabled key pointing at a non-existent
# account renders a broken Sponsor button. Uncomment and set real targets once
# real GitHub Sponsors / Ko-fi project accounts exist (admin step — see GOVERNANCE.md),
# then delete the unused keys.
#
# TODO(handle): enable funding.
# github: [AgentClash]
# custom: ["https://www.agentclash.dev"]

# Frontend Architecture

This folder will contain:

- `stack.md` — Next.js App Router, data fetching strategy, client vs server components
- `api-mapping.md` — which frontend page calls which backend API, request/response shapes
- `auth-flow.md` — WorkOS integration, session management, protected routes

These docs will be written when the dashboard pages are built. The design system comes first.

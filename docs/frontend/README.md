# Frontend Documentation

```
docs/frontend/
├── design/
│   ├── design-system.md          ← tokens, typography, color, spacing
│   ├── page-inventory.md         ← every page the frontend needs, mapped to backend APIs
│   └── component-patterns.md     ← reusable patterns (tables, badges, states)
│
└── architecture/
    ├── (planned) stack.md        ← Next.js app router, data fetching, auth
    ├── (planned) api-mapping.md  ← which frontend page calls which backend API
    └── (planned) auth-flow.md    ← WorkOS integration, session management
```

## Status

- `design/` — established from landing page work, ready to guide dashboard development
- `architecture/` — placeholder, will be filled when dashboard pages are built
